<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.springboot.project.domain.user.MovieRepository">
  	
  	<select id="getChartAll" resultType="com.springboot.project.domain.user.Movie"> <!-- 영화 목록 list read -->
  		select
  			mov_idn,
  			mov_photo,
  			mov_title,
  			mov_release,
  			mov_viewCnt,
  			'0',
  			mov_rat,
  			'0',
  			'0',
  			mov_lik_cnt,
  			'0',
  			'0',
  			'0',
  			'0'
  			
  		from
  			movie_mst
  		order by
  			mov_idn
  	</select>
  	<select id="getChartAllDesc" resultType="com.springboot.project.domain.user.Movie"> <!-- 영화 목록 list read -->
  		select
  			mov_idn,
  			mov_photo,
  			mov_title,
  			mov_release,
  			mov_viewCnt,
  			'0',
  			mov_rat,
  			'0',
  			'0',
  			mov_lik_cnt,
  			'0',
  			'0',
  			'0',
  			'0'
  			
  		from
  			movie_mst
  		order by
  			mov_idn desc
  	</select>
  	<select id="getChartAllRelease" resultType="com.springboot.project.domain.user.Movie"> <!-- 영화 목록 list read -->
  		select
  			mov_idn,
  			mov_photo,
  			mov_title,
  			mov_release,
  			mov_viewCnt,
  			'0',
  			mov_rat,
  			'0',
  			'0',
  			mov_lik_cnt,
  			'0',
  			'0',
  			'0',
  			'0'
  			
  		from
  			movie_mst
  		order by
  			mov_release
  	</select>
  	<select id="getChartAllReleaseDesc" resultType="com.springboot.project.web.model.dto.MainChartDto"> <!-- 영화 목록 list read -->
  		select
  			mov_idn,
  			mov_photo,
  			mov_title,
  			mov_release,
  			mov_viewCnt,
  			'0',
  			mov_rat,
  			'0',
  			'0',
  			mov_lik_cnt,
  			'0',
  			'0',
  			'0',
  			'0'
  			
  		from
  			movie_mst
  		order by
  			mov_release desc
  	</select>
  	<select id="getChartAllLike" resultType="com.springboot.project.web.model.dto.MainChartDto"> <!-- 영화 목록 list read -->
  		select
  			mov_idn,
  			mov_photo,
  			mov_title,
  			mov_release,
  			mov_viewCnt,
  			'0',
  			mov_rat,
  			'0',
  			'0',
  			mov_lik_cnt,
  			'0',
  			'0',
  			'0',
  			'0'
  			
  		from
  			movie_mst
  		order by
  			mov_lik_cnt
  	</select>
  	<select id="getChartAllLikeDesc" resultType="com.springboot.project.web.model.dto.MainChartDto"> <!-- 영화 목록 list read -->
  		select
  			mov_idn,
  			mov_photo,
  			mov_title,
  			mov_release,
  			mov_viewCnt,
  			'0',
  			mov_rat,
  			'0',
  			'0',
  			mov_lik_cnt,
  			'0',
  			'0',
  			'0',
  			'0'
  			
  		from
  			movie_mst
  		order by
  			mov_lik_cnt desc
  	</select>
  	
  	
  	
  	
 
	
  </mapper>